# План исправления критических ошибок

Дата: 2026-07-18  
Статус: В работе

## Обнаруженные проблемы

### 1. Поиск возвращает неверные документы при большом индексе
**Симптомы:** На 7 книгах работает корректно. На 100 книгах (135 069 чанков) находит документы, не содержащие искомое слово.
**Гипотеза:** Пересечение данных в postings.idx при многократных вызовах grow() во время индексации, либо неверное восстановление блоков при загрузке lexicon.
**Приоритет:** Критический

### 2. Подсчёт символов в чанках неверен
**Симптомы:** `char_count` в БД показывает количество байт, а не UTF-8 символов. Кириллица занимает 2 байта на символ.
**Причина:** Используется `content.size()` (байты) вместо подсчёта code points. Также `word_count` считается в токенах, а не в словах исходного текста.
**Приоритет:** Высокий

### 3. Чанкер разрывает слова
**Симптомы:** Чанк заканчивается на `ме-` — слово разорвано на границе чанка.
**Причина:** Текущий алгоритм разбивает текст по длине в байтах, а не по предложениям. Не учитывает границы предложений при склеивании/разбиении.
**Приоритет:** Высокий

### 4. doc_id начинается с 0
**Симптомы:** Первый документ имеет doc_id=0.
**Причина:** `last_doc_id_` инициализируется нулём в `IndexWriter::open()`.
**Приоритет:** Низкий (косметический)

---

## План исправлений

### Этап 2.8.1: Исправить doc_id (проблема 4)
- [ ] Изменить `last_doc_id_` с 0 на 1 в `IndexWriter::open()`
- [ ] Проверить восстановление из SQLite (MAX(doc_id) + 1 при загрузке)

### Этап 2.8.2: Исправить подсчёт символов (проблема 2)
- [ ] Реализовать функцию `utf8_length()` для подсчёта code points
- [ ] Заменить `content.size()` на `utf8_length(content)` в `IndexWriter::addDocument()`
- [ ] Исправить `word_count` — должен считать слова (после токенизации это количество токенов, что корректно)

### Этап 2.8.3: Исправить чанкер — разбивка по предложениям (проблема 3)
**Новая логика (на основе Go-кода):**
1. Вход: текст книги с параграфами, разделёнными `\n`
2. Для каждого параграфа:
   - Если длина в UTF-8 символах > `max_chunk_size`:
     а) Разбить параграф на предложения (`splitSentences`)
     б) Группировать предложения в чанки до `opt_chunk_size`
     в) Если предложений нет (нет знаков препинания) — разбить по словам с ограничением `max_chunk_size`
     г) Одиночное предложение длиннее `max_chunk_size` остаётся как есть (не разрывается)
   - Если длина <= `max_chunk_size`:
     а) Попытаться склеить с текущим накопительным чанком
     б) Если сумма > `max_chunk_size` — сохранить накопленный, начать новый
     в) Если сумма >= `opt_chunk_size` — сохранить
     г) Если сумма < `min_chunk_size` — продолжить склеивать
3. Сохранить оставшийся чанк

**Особые случаи:**
- Троеточия нормализуются (`...` → `…`)
- Пустые параграфы пропускаются
- Размеры всегда в UTF-8 символах

### Этап 2.8.4: Отладка поиска (проблема 1)
- [ ] Добавить отладочный вывод в `findTerm` для выбранного терма
- [ ] Проверить, какие doc_id хранятся в индексе для терма "абаза"
- [ ] Сравнить с реальными doc_id чанков, содержащих "абаза" в БД
- [ ] Найти корневую причину несоответствия
- [ ] Исправить

### Этап 2.8.5: Финальное тестирование
- [ ] Переиндексировать 100 книг
- [ ] Проверить поиск по запросам: "абаза", "триллиум", "москва", "крокус"
- [ ] Сравнить результаты с содержимым чанков через SQL
- [ ] Пройти все 55 unit-тестов

---

## Порядок выполнения
1. 2.8.1 (doc_id) — простой фикс, немедленный эффект
2. 2.8.2 (подсчёт символов) — простой фикс
3. 2.8.3 (чанкер) — средняя сложность, требует переписывания
4. 2.8.4 (отладка поиска) — самый сложный, требует исследования на большом индексе
5. 2.8.5 (финальное тестирование) — проверка всех исправлений вместе